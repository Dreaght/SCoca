package com.megadev.scoca.command;

import dev.mega.megacore.command.Argument;
import dev.mega.megacore.command.matcher.ArgumentMatcher;

public class SrpCommand extends Argument {
    public SrpCommand(ArgumentMatcher matcher) {
        super(matcher);
    }
}